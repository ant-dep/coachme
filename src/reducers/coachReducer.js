import {CONNECT_COACH, LOGOUT_COACH} from '../actions/coach/actions-types';

const initialState = {
    infos: {},
    isLogged: false
}

export default function CoachReducer(state = initialState, action) {
    
    
    return state;
}