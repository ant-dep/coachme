import {LOAD_LESSONS} from '../actions/lesson/actions-types';
import moment from 'moment'


const initialState = {
    allLessons: [],
    futurLessons: [],
    oldLessons: []
}

export default function LessonReducer(state = initialState, action) {
    
    
    return state;
}