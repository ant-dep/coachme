import {CONNECT_COACH, LOGOUT_COACH} from "./actions-types"

// action de connexion des users
export const connectCoach = (coach) =>{
    
    
}

// action de deconnexion des users
export const logoutCoach = () =>{
    
    
    
}